BIT question

1. if we log and get an integer it will be powe of Two

bool isPowerOfTwo(int n)
{
   if(n==0)
   return false;
 
   return (ceil(log2(n)) == floor(log2(n)));
}
bool isPowerOfTwo (int x)
{
    return x && (!(x&(x-1)));
}